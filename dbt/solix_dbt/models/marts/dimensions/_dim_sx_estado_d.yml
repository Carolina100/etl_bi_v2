version: 2

models:
  - name: dim_sx_estado_d
    description: "Dimensao DW de estados alimentada a partir do DS."
    columns:
      - name: SK_ESTADO
        tests:
          - not_null
          - unique
      - name: ID_CLIENTE
        tests:
          - not_null
      - name: CD_ESTADO
        tests:
          - not_null
